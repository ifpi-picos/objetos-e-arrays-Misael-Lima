//Crie uma string com uma frase e use o método `toUpperCase` para imprimir a frase em letras maiúsculas.

const nome = ('Misael Lima')
const nomeMaiusculo = nome.toUpperCase()

console.log(nomeMaiusculo)